name: Claude Code Warmup (acc1)

on:
  schedule:
    - cron: '30 5 * * *'  # 07:30 Polish time (CEST, summer)
    - cron: '30 6 * * *'  # 07:30 Polish time (CET, winter)
  workflow_dispatch: {}

concurrency:
  group: claude-warmup-acc1
  cancel-in-progress: false

jobs:
  warmup:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Validate configuration
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_OAUTH_TOKEN }}
        run: |
          if [ -z "${CLAUDE_CODE_OAUTH_TOKEN}" ]; then
            echo "CLAUDE_OAUTH_TOKEN secret is not set."
            exit 1
          fi

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Ping Claude to anchor 5-hour window
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_OAUTH_TOKEN }}
        run: |
          mkdir -p ~/.claude
          echo '{"hasCompletedOnboarding":true}' > ~/.claude.json
          output_file="$(mktemp)"
          set +e
          claude -p "hi" --model haiku --no-session-persistence >"$output_file" 2>&1
          status=$?
          set -e
          cat "$output_file"

          if [ $status -eq 0 ]; then
            echo "✓ acc1 warmup complete"
            exit 0
          fi

          if grep -Eiq "expired|unauthorized|invalid token|authentication|auth failed|login required|not logged in" "$output_file"; then
            echo "Token invalid/expired. Refresh: claude setup-token + gh secret set CLAUDE_OAUTH_TOKEN"
            exit 1
          fi

          if grep -qi "rate limit" "$output_file"; then
            echo "Rate limit; treating as success."
            exit 0
          fi

          exit $status
